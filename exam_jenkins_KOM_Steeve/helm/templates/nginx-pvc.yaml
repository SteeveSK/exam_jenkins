apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    service: nginx-pvc
  name: nginx-pvc
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
